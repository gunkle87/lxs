INPUT(a0)
INPUT(b0)
INPUT(a1)
INPUT(b1)
INPUT(a2)
INPUT(b2)
INPUT(a3)
INPUT(b3)
INPUT(cin)
OUTPUT(sum0)
OUTPUT(sum1)
OUTPUT(sum2)
OUTPUT(sum3)
OUTPUT(cout)

xor0 = XOR(a0, b0)
sum0 = XOR(xor0, cin)
and0a = AND(a0, b0)
and0b = AND(xor0, cin)
carry0 = OR(and0a, and0b)

xor1 = XOR(a1, b1)
sum1 = XOR(xor1, carry0)
and1a = AND(a1, b1)
and1b = AND(xor1, carry0)
carry1 = OR(and1a, and1b)

xor2 = XOR(a2, b2)
sum2 = XOR(xor2, carry1)
and2a = AND(a2, b2)
and2b = AND(xor2, carry1)
carry2 = OR(and2a, and2b)

xor3 = XOR(a3, b3)
sum3 = XOR(xor3, carry2)
and3a = AND(a3, b3)
and3b = AND(xor3, carry2)
cout = OR(and3a, and3b)
